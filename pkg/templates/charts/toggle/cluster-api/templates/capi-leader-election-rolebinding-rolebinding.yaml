apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  labels:
    cluster.x-k8s.io/provider: cluster-api
  name: '{{ .Values.org }}:{{ .Chart.Name }}:cluster-api'
  namespace: capi-system
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: '{{ .Values.org }}:{{ .Chart.Name }}:cluster-api'
subjects:
- kind: ServiceAccount
  name: capi-manager
  namespace: capi-system
